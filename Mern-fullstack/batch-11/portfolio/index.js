let friut = "apple";
let snacks = "noodles";
const arrowFuncion = () => console.log("arrow funcion called");

function namedFuncion(arrowFuncion) {
  // parameters (params)
  arrowFuncion();
}

let furitsList = ["apple", "orange"];

furitsList.map((data, index) => console.log(data, index));
